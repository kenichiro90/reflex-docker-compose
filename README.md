# How to Run

```sh
> mkdir workspace
> docker compose build
> docker compose up -d
> docker compose exec python bash

> (docker) cd workspace
> (docker) mkdir my_first_app
> (docker) cd my_first_app
> (docker) reflex init
> (docker) reflex run
```
